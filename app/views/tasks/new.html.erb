<%= form_for @task, url: tasks_path, method: 'post' do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>

  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit %>
<% end %>
